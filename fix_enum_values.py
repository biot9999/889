#!/usr/bin/env python3
"""
MongoDB 不需要枚举值修复
MongoDB does not require enum value fixing

说明：
MongoDB 作为文档数据库，存储的是字符串值而不是枚举类型，
因此不需要像 SQLite 那样修复枚举值问题。

Note:
MongoDB as a document database stores string values instead of enum types,
so there is no need to fix enum value issues like in SQLite.
"""
print("ℹ️  MongoDB 不需要枚举值修复")
print("ℹ️  MongoDB does not require enum value fixing")

