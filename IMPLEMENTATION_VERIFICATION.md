# 实现验证指南 (Implementation Verification Guide)

本文档提供了所有实现功能的验证清单和测试步骤。

## ✅ 验证清单

### 1️⃣ 配置消息自动删除

**功能描述**: 配置完成后 3 秒自动删除提示消息、用户输入和确认消息

**测试步骤**:
1. 进入任务配置界面
2. 点击"🧵 配置线程数"按钮
3. 输入数字（例如：5）
4. 观察以下消息是否在 3 秒后自动删除：
   - "🧵 配置线程数" 提示消息
   - 用户输入的数字消息
   - "✅ 线程数已设置为：5" 确认消息

**预期结果**: ✅ 所有三条消息在 3 秒后消失

**适用配置项**:
- ✅ 线程数配置
- ✅ 发送间隔配置
- ✅ 无视双向次数配置

---

### 2️⃣ 进度显示优化

**功能描述**: 任务启动后立即显示初始进度，不再全是 0

**测试步骤**:
1. 创建一个任务并添加目标用户
2. 启动任务
3. 观察进度显示：
   - 任务启动时应显示 0/总数 (0.0%)
   - **1 秒后**应自动刷新显示实际进度（如 1/20 (5.0%)）
4. 点击"🔄 刷新进度"按钮应立即响应
5. 观察自动刷新间隔是否为 30-60 秒随机

**预期结果**: 
- ✅ 启动后 1 秒内显示实际进度
- ✅ 手动刷新立即响应
- ✅ 自动刷新间隔 30-60 秒随机

---

### 3️⃣ 线程逻辑重构 - 重复发送模式

**功能描述**: 勾选"重复发送"后，所有账号轮流给所有用户发送消息

**测试场景**:
```
测试数据：
- 用户名单：20 个用户
- 账号数量：20 个账号
- 线程数：5
- 勾选：重复发送
```

**预期执行流程**:
```
第 1 轮：账号 1-5 → 同时发送给用户 1-20（每个账号发 20 条）
第 2 轮：账号 6-10 → 同时发送给用户 1-20（重复发送）
第 3 轮：账号 11-15 → 同时发送给用户 1-20（重复发送）
第 4 轮：账号 16-20 → 同时发送给用户 1-20（重复发送）
```

**验证要点**:
- ✅ 每个用户收到 20 条消息（来自 20 个不同账号）
- ✅ 使用 5 个账号并发，分 4 轮完成
- ✅ 日志显示"执行模式：重复发送"
- ✅ 日志显示分批信息："账号分批: 4 批"

**日志关键字**:
```
执行模式：重复发送
账号分批: 4 批，每批 5 个账号
第 1/4 轮
使用账号: ['+1234567890', ...]
```

---

### 4️⃣ 线程逻辑重构 - 正常模式

**功能描述**: 未勾选"重复发送"，按名单依次发送，失败跳过换账号

**测试场景**:
```
测试数据：
- 用户名单：20 个用户
- 账号数量：20 个账号
- 线程数：5
- 未勾选：重复发送
```

**预期执行流程**:
```
用户 1：账号 1 尝试 → 成功 ✓
用户 2：账号 2 尝试 → 失败 ✗ → 账号 3 尝试 → 成功 ✓
用户 3：账号 4 尝试 → 成功 ✓
...
用户 20：根据前面的成功/失败情况使用可用账号
```

**验证要点**:
- ✅ 每个用户最多收到 1 条消息
- ✅ 失败的用户会尝试多个账号直到成功
- ✅ 目标按顺序处理（不重复发送）
- ✅ 日志显示"执行模式：正常模式"
- ✅ 日志显示尝试不同账号的记录

**日志关键字**:
```
执行模式：正常模式
[批次 0] 使用账户 +1234567890 尝试发送
[批次 0] 账户 +1234567890 发送失败，尝试下一个账户
[批次 0] ✅ 发送成功
```

---

### 5️⃣ 账户状态检查

**功能描述**: 调用 @spambot 检查所有账户状态并分类

**测试步骤**:
1. 进入"📱 账户管理"菜单
2. 点击"🔍 检查账户状态"
3. 等待检查完成（每个账号约 2-3 秒）
4. 查看统计结果：
   - ✅ 无限制账号数量
   - ⚠️ 双向限制账号数量
   - ❄️ 冻结账号数量
   - 🚫 封禁账号数量

**预期结果**:
- ✅ 显示"正在调用 @spambot 检查所有账户..."
- ✅ 完成后显示分类统计
- ✅ 账户状态在数据库中更新
- ✅ 显示导出按钮

**@spambot 响应解析（增强版）**:

**优先级分类系统**:
```
优先级1: 地理限制
  "some phone numbers may trigger a harsh response" → ACTIVE (地理限制，非双向限制)

优先级2: 临时限制
  "account is now limited until" / "temporarily limited" → LIMITED (临时限制)
  "暂时限制" (中文支持)

优先级3: 永久封禁
  "permanently banned" / "banned permanently" → BANNED (永久封禁)
  "永久封禁" (中文支持)

优先级4: 垃圾邮件限制
  "actions can trigger a harsh response" / "peer flood" → LIMITED (垃圾邮件)
  "you can only" → LIMITED (双向限制)

优先级5: 等待验证
  "wait" / "pending" / "verification" → RESTRICTED (等待验证)
  "审核中" (中文支持)

优先级6: 无限制
  "good news, no limits" / "no restrictions" → ACTIVE (无限制)
  "正常" (中文支持)

特殊情况:
  无响应 / 连接失败 → BANNED (死亡账户)
```

**多语言支持**: 自动支持中文和英文关键词匹配

---

### 6️⃣ 账户导出功能

**功能描述**: 导出账户文件为 ZIP 格式

#### 测试 A: 导出所有账户

**测试步骤**:
1. 完成账户状态检查
2. 点击"📥 全部账户提取"
3. 下载 ZIP 文件
4. 解压并验证内容

**预期结果**:
- ✅ 文件名格式：`accounts_all_YYYYMMDD_HHMMSS.zip`
- ✅ 包含所有账户的 .session 文件
- ✅ 包含对应的 .json 文件（如果存在）
- ✅ 显示总账户数

#### 测试 B: 导出受限账户

**测试步骤**:
1. 完成账户状态检查
2. 点击"⚠️ 受限账户提取"
3. 如果有受限账户，下载 ZIP 文件
4. 解压并验证只包含受限账户

**预期结果**:
- ✅ 文件名格式：`accounts_limited_YYYYMMDD_HHMMSS.zip`
- ✅ 只包含 LIMITED、BANNED、INACTIVE 状态的账户
- ✅ 如果没有受限账户，显示"✅ 没有受限账户"
- ✅ 显示受限账户数量

---

## 🔍 日志验证

### 重复发送模式日志示例
```
================================================================================
开始执行任务
任务ID: 507f1f77bcf86cd799439011
任务名称: 测试任务
发送方式: direct
线程数配置: 5
重复发送模式: True
================================================================================
执行模式：重复发送
目标用户数: 20
可用账号数: 20
线程数: 5
================================================================================
账号分批: 4 批，每批 5 个账号
================================================================================
第 1/4 轮
使用账号: ['+1234567890', '+0987654321', ...]
================================================================================
账号 +1234567890 开始给所有用户发送
账号 +1234567890 -> 用户 username1 (1/20)
✅ 发送成功
...
第 1 轮完成
```

### 正常模式日志示例
```
================================================================================
开始执行任务
任务ID: 507f1f77bcf86cd799439011
任务名称: 测试任务
发送方式: direct
线程数配置: 5
重复发送模式: False
================================================================================
执行模式：正常模式
目标用户数: 20
可用账号数: 20
线程数: 5
================================================================================
目标分批: 4 批，每批约 5 个目标
批次 1: 分配账户 +1234567890，处理 5 个目标
================================================================================
[批次 0] 开始处理 5 个目标
[批次 0] 处理目标 1/5: username1
[批次 0] 使用账户 +1234567890 尝试发送
[批次 0] ✅ 发送成功
...
[批次 0] 批次处理完成
```

---

## 🐛 已知问题和注意事项

1. **账户状态检查**
   - 每个账户检查需要 2-3 秒
   - 大量账户（>50）可能需要较长时间
   - 网络问题可能导致部分账户检查失败

2. **消息自动删除**
   - 需要机器人有删除消息权限
   - 私聊中总是有权限
   - 群组中可能需要管理员权限

3. **进度刷新**
   - 自动刷新间隔 30-60 秒随机
   - 手动刷新不受限制
   - Telegram API 有编辑消息频率限制（注意不要过于频繁手动刷新）

4. **账户导出**
   - 只导出存在的 .session 文件
   - tdata 格式文件不包含在导出中（需要单独处理）
   - ZIP 文件临时存放在 results 目录

---

## ✅ 完整功能清单

- [x] 配置消息 3 秒后自动删除（提示、输入、确认）
- [x] 任务启动后立即显示进度（1秒后刷新）
- [x] 进度每 30-60 秒随机刷新
- [x] 重复发送模式：分批账号→所有用户
- [x] 正常模式：按名单依次发送，失败换账号
- [x] 检查账户状态：调用 @spambot 并解析
- [x] 导出全部账户：生成 ZIP 文件
- [x] 导出受限账户：只导出受限的账号

---

## 📊 代码统计

- **总代码行数**: 3646 行
- **新增代码**: +489 行
- **修改代码**: -40 行
- **净增加**: +449 行

### 主要新增功能函数

1. `_execute_repeat_send_mode()` - 重复发送模式执行逻辑
2. `_execute_normal_mode()` - 正常模式执行逻辑
3. `_process_batch_normal_mode()` - 正常模式批处理
4. `check_all_accounts_status()` - 账户状态检查
5. `export_accounts()` - 账户导出功能

### 修改的核心函数

1. `_execute_task()` - 重构为支持两种模式
2. `start_task_handler()` - 添加立即进度刷新
3. `_monitor_progress()` - 随机间隔 30-60 秒
4. `request_thread_config()` - 存储提示消息 ID
5. `request_interval_config()` - 存储提示消息 ID
6. `request_bidirect_config()` - 存储提示消息 ID
7. `handle_thread_config()` - 删除提示消息
8. `handle_interval_config()` - 删除提示消息
9. `handle_bidirect_config()` - 删除提示消息
10. `show_accounts_menu()` - 显示账户统计
11. `button_handler()` - 添加新的按钮处理逻辑

---

## 🔧 技术实现细节

### 配置消息自动删除
```python
# 存储提示消息 ID
prompt_msg = await query.message.reply_text(...)
context.user_data['config_prompt_msg_id'] = prompt_msg.message_id

# 3 秒后删除所有消息
await asyncio.sleep(CONFIG_MESSAGE_DELETE_DELAY)  # 3 seconds
await msg.delete()  # 确认消息
await update.message.delete()  # 用户输入
await context.bot.delete_message(chat_id, prompt_msg_id)  # 提示消息
```

### 进度立即刷新
```python
# 显示初始进度
progress_msg = await query.message.reply_text(...)

# 等待 1 秒后更新
await asyncio.sleep(1)
task_doc = db[Task.COLLECTION_NAME].find_one(...)
await progress_msg.edit_text(...)
```

### 随机刷新间隔
```python
async def _monitor_progress(self, task_id):
    while True:
        interval = random.randint(30, 60)  # 30-60 秒随机
        await asyncio.sleep(interval)
```

### 重复发送模式
```python
# 分批账号
account_batches = [accounts[i:i + batch_size] 
                   for i in range(0, len(accounts), batch_size)]

# 每批账号并发发送
for batch in account_batches:
    await asyncio.gather(*[send_to_all_targets(acc) for acc in batch])
```

### 正常模式
```python
# 每个目标尝试多个账号
for target in targets:
    success = False
    attempts = 0
    while not success and attempts < max_attempts:
        account = account_pool[account_index % len(account_pool)]
        success = await self._send_message(task, target, account)
        if not success:
            account_index += 1
            attempts += 1
```

---

## 📝 总结

本次实现完成了所有需求的核心功能：
1. ✅ UI 优化（消息自动删除、进度立即显示）
2. ✅ 核心逻辑重构（两种发送模式）
3. ✅ 账户管理增强（状态检查、批量导出）

所有功能都已实现并通过语法检查。建议在实际运行环境中进行完整测试，验证所有功能按预期工作。
